apiVersion: v1
stringData:
  USERNAME: {{.mirrorUsername}}
  PASSWORD: {{.mirrorPassword}}
  CACERTCONTENT: |
    {{- range $line := .mirrorCACertContent}}
    {{ $line }}
    {{- end}}
kind: Secret
metadata:
  name: registry-mirror-secret
  namespace: eksa-packages
type: Opaque
