server = "https://{{ .server }}"

[host."https://{{ .host }}"]
  capabilities = ["pull", "resolve"]
  override_path = true
{{- if .registryCACert }}
  ca = "/etc/containerd/certs.d/{{ .mirrorBase }}/ca.crt"
{{- end }}
{{- if .insecureSkip }}
  skip_verify = true
{{- end }}
