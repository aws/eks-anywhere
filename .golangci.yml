run:
  timeout: 5m
  skip-files:
  - "zz_generated.*\\.go$"
  skip-dirs:
  - ".*/mocks"
  - "manager/tilt_modules"
linters:
  enable:
    - gofumpt
    - gci
linters-settings:
  gci:
    local-prefixes: github.com/aws/eks-anywhere
issues:
  max-same-issues: 0
  max-issues-per-linter: 0
